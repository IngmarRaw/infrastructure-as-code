---
plugin: azure.azcollection.azure_rm
auth_source: cli

include_vm_resource_groups:
  - S1073133

plain_host_names: true

conditional_groups:
  role_webserver: "'role' in tags and tags.role == 'webserver' and 'project' in tags and tags.project == 'les-05'"