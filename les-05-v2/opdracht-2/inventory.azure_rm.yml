---
plugin: azure.azcollection.azure_rm
auth_source: cli

include_vm_resource_groups:
  - S1073133

plain_host_names: true

keyed_groups:
  - key: tags.role
    prefix: role
    separator: "_"

compose:
  vm_tags: tags
  vm_name: name